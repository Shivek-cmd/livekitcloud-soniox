[project]
name = "livekit-sarvam-restaurant"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[sarvam]>=1.0,<2.0",
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "twilio>=9.0",
]
